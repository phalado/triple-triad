import { StyleSheet } from 'react-native';

const AppStyles = StyleSheet.create({
  container: {
    backgroundColor: '#AAA',
    width: '100%',
    height: '100%',
  },
  text: {
    color: 'blue',
    fontSize: 20,
  },
});

export default AppStyles;
